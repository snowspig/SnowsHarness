# File Organization

## 原则

- ❌ 项目根目录不放散文件

## 工作文件放置

- 计划/规格/设计文档 → `docs/`
- 临时脚本 (.py, .ps1, .sh 快速测试) → `tmp/`
- 生成代码 → 适当 `src/` 或项目子目录

## 清理规则

- `tmp/` 文件可丢弃 → 任务完成时清理
- 不留 `debug_*`, `test_*`, 临时 .ps1/.py 脚本在根目录

## 不确定时

- 先问，不默认创建
